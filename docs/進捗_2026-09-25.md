# 進捗メモ 2026-09-25

## できたこと

- **Step 0 棚卸し**（`inventory/棚卸し_大会一覧.xlsx`）
  - FIS DB カレンダー: 1990-91〜2025-26 の 36 シーズン、1,133 大会（MO を含む大会）。取り込み範囲の区分つき。
    - 全部: JPN 開催の FIS/NC/OPN/AC・ANC・WJC・IYC など 201 大会
    - 日本人出場分のみ: EC・NAC 283 大会（出場の有無は未確認）
    - 対象外: W杯・世界選手権・五輪 299、海外の国内戦など 350
  - SAJ 競技データバンク: 2011-12〜2025-26 の 15 シーズン、MO/DM で 1,132 行（性別×競技日）、うち結果 PDF あり 871、HTML 順位表あり 1,132。
    等級の内訳: A級 543、B級 416、FIS NC 60、W杯 48、ジュニア 28、その他。
  - SAJ の結果 PDF は A級・B級・全日本ジュニア・FIS/A とも全日本と同じ様式（SAJ-FM）で、審判別の点がある（4件で確認）。
    既存の全日本パーサーがそのまま読めた（B級の一部に減点欄なしの様式あり）。
- **Step 1**
  - リポジトリ `mic-results`（ローカル、main、未 push）。moguls-results の ETL を registry 駆動・アダプタ方式に一般化。
  - 全日本 2017〜2026 の 10 大会・38 ラウンド・833 本を取り込み、多層照合エラー 0（警告 11 件は規則ファイルの例外・ペースタイム注記・表記ゆれ）。
    正解データ 89 本一致。変異テスト 13 種すべて検出。
  - 画面 4 つ（リザルト・大会・選手・データについて）。PC 幅と 375px 幅で JS エラーなし、横スクロールなし。

## できていないこと・注意

- **FIS の大会詳細（レース一覧・PDF）は未取得。** カレンダー 36 ページを短時間に開いた直後、ブラウザからの FIS へのアクセスが 403 になった
  （curl では 200 なのでブラウザセッション単位の制限とみられる）。回避はせず、別日に人の速度（ページ間に数秒以上）で取り直す。
- EC・NAC の「日本人出場」判定は未実施。日本人選手の FIS バイオグラフィーの出場履歴から逆引きする案が現実的。
- 第5層（SAJ データバンクの HTML 順位表との照合）は未実装。順位表は全年に存在するので実装可能。
- `etl/mic_roster.json` は空。MIC 選手カルテの名簿シートから氏名と在籍期間を写す必要がある（Van さん）。
- 選手の同定: 全日本は全員 FIS コードがあった。A級・B級・ジュニアは FIS コードのない選手が出るため、`saj-<番号>` ID と `athlete_master.json` の運用が必要。
- 公開（GitHub の公開リポジトリ作成と Pages）は Van さんの確認後。

## 次にやること（指示書の作業順）

1. FIS の大会詳細を人の速度で取得（Step 0 の残り）。
2. Step 2: 国内 FIS レース・ANC・WJC（FIS 様式。`etl/adapters/fis_pdf/` を registry に接続する）。
3. Step 4 の先行: SAJ の A級・B級・ジュニア（同じアダプタ。PDF を保存して registry に登録するだけ。減点欄なしの様式の扱いを決める）。
4. 第5層（SAJ データバンク HTML）と MIC 名簿。

## 追記（同日午後）: Step 6 W杯・世界選手権・五輪の流用

- `etl/adapters/moguls_results/` を追加。moguls-results の公開データ（data/*.json）を得点段階（score）で写す。
  `python -m etl.adapters.moguls_results.sync_registry` が registry（32 大会・192 ラウンド）・正解データ（31 本）・
  第5層の結果（FIS 公式 Web との照合 ok 188 / upstream_missing 4）・確認済みの別名（28 件）を生成する。
- 全体ビルド: 42 大会 / 230 ラウンド / 5,246 本、エラー 0、警告 38（全日本 11 ＋ moguls-results 側で既知の 27: 欠場・シード直接進出・国籍変更・表記ゆれ）。
  正解データ 120 本一致、変異テスト 13/13。
- 選手の統合: FIS コードが同じ全日本と W杯の記録は1人にまとまる（両方に出た選手 139 名）。表示名は日本語表記を優先し、
  ローマ字表記は別名（検索可）。所属の履歴は FIS 様式（所属の印字なし）で途切れない。
- 画面: 大会ページの出典を「男子 予選」のようにラウンド名で示し、moguls-results の同じラウンド（審判点あり）へのリンクを
  検証の詳細に出す。大会一覧カードで会場名が2回出ないようにした。
- 未実施のまま: FIS 大会詳細の取得（403 の翌日以降に人の速度で）、Step 2〜5、第5層（SAJ HTML）、MIC 名簿、公開。
